node_modules/
dist/
.superpowers/
# Only the repo-root user-data folder. A bare `profile/` also matches
# packages/controller/src/components/profile/ and would drop the editor UI from git.
/profile/*
!/profile/.gitkeep
.env
*.log
